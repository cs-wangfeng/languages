# What is Zola

- Zola 是一个静态站点生成器 (SSG)

> SSG 使用动态模板将内容转换为静态 HTML 页面。因此，静态站点非常快且不需要数据库，因此易于托管。

- 它是用Rust编写的.

- 使用Tera模板引擎，类似于Jinja2、Django 模板、Liquid和Twig。

- 内容是用CommonMark编写的，它是Markdown的一个强定义、高度兼容的规范。

